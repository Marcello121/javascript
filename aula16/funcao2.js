function soma (n1=0, n2=0){ // Parametros opicionais, nesse caso, se eu não colocar um definir um dos dois números, ele vai considerar como 0 (zero)
    return n1 + n2
}

console.log(soma(3)) // O parametrô é como se fosse uma varíavel a ser definda/a ser atribuída algum valor?